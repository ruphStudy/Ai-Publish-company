import { InsightCategory, InsightScope } from '../entities/ai-insight.entity';

export interface InsightPromptTemplate { templateKey: string; version: string; scope: InsightScope; category: InsightCategory; systemPrompt: string; userTemplate: string; active: boolean }
export interface AIInsightsPolicy { policyVersion: string; enabledAIProviders: string[]; aiProviderPriority: string[]; defaultProvider: string; defaultModel: string; temperature: number; maxTokens: number; promptVersion: string; confidenceThreshold: number; duplicateSuppressionDays: number; cacheTtlSeconds: number; maximumRetries: number; templates: InsightPromptTemplate[] }
export const aiInsightsDefaultPolicy: AIInsightsPolicy = { policyVersion: 'ai-insights-default-v1', enabledAIProviders: ['mock', 'openai'], aiProviderPriority: ['mock', 'openai'], defaultProvider: 'mock', defaultModel: 'mock-insight-model', temperature: 0.2, maxTokens: 1200, promptVersion: 'insight-prompts-v1', confidenceThreshold: 35, duplicateSuppressionDays: 30, cacheTtlSeconds: 300, maximumRetries: 3, templates: [{ templateKey: 'executive-summary', version: '1', scope: InsightScope.PORTFOLIO, category: InsightCategory.EXECUTIVE_SUMMARY, systemPrompt: 'Return structured JSON business insights. Do not execute actions.', userTemplate: 'Analyze the supplied analytics and opportunities into explainable insights.', active: true }] };
