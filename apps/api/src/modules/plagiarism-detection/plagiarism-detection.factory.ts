import { randomUUID } from 'crypto';
import { Injectable } from '@nestjs/common';
import { PlagiarismDetection, PlagiarismDetectionScope, PlagiarismDetectionStatus, PlagiarismRiskLevel, PlagiarismTargetType, PublicationRecommendation } from './entities/plagiarism-detection.entity';
@Injectable()
export class PlagiarismDetectionFactory {
  create(input: { projectId: string; targetType: PlagiarismTargetType; targetId: string; scope: PlagiarismDetectionScope; manuscriptVersion: string; detectionVersion: number; createdBy?: string }, result: { originalityScore: number; similarityScore: number; duplicateScore: number; copyrightRiskScore: number; aiRepetitionScore: number; findings: PlagiarismDetection['findings']; matchedSections: PlagiarismDetection['matchedSections']; duplicateLocations: string[]; recommendations: string[]; provider: string; model: string; tokenUsage: Record<string, number>; estimatedCost: number; processingTime: number }): Partial<PlagiarismDetection> { const maximum = Math.max(result.similarityScore, result.duplicateScore, result.copyrightRiskScore, result.aiRepetitionScore); const riskLevel = maximum >= 85 ? PlagiarismRiskLevel.CRITICAL : maximum >= 65 ? PlagiarismRiskLevel.HIGH : maximum >= 40 ? PlagiarismRiskLevel.MEDIUM : maximum > 0 ? PlagiarismRiskLevel.LOW : PlagiarismRiskLevel.NONE; const publicationRecommendation = riskLevel === PlagiarismRiskLevel.CRITICAL ? PublicationRecommendation.BLOCK : riskLevel === PlagiarismRiskLevel.HIGH ? PublicationRecommendation.REVISE : riskLevel === PlagiarismRiskLevel.MEDIUM ? PublicationRecommendation.REVIEW : PublicationRecommendation.PUBLISH; return { detectionId: `PLG-${new Date().toISOString().slice(0, 10).replace(/-/g, '')}-${randomUUID().slice(0, 8).toUpperCase()}`, ...input, ...result, riskLevel, publicationRecommendation, status: PlagiarismDetectionStatus.COMPLETED, failureCode: null, failureMessage: null, metadata: { internetSearchPerformed: false, externalProviderUsed: false }, isDeleted: false, createdBy: input.createdBy ?? null, updatedBy: input.createdBy ?? null }; }
}
