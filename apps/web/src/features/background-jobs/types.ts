export type JobExecutionStatus = 'PENDING' | 'SCHEDULED' | 'QUEUED' | 'RUNNING' | 'PAUSED' | 'CANCELLING' | 'CANCELLED' | 'SUCCEEDED' | 'FAILED' | 'RETRYING' | 'TIMED_OUT' | 'DEAD_LETTERED' | 'PARTIALLY_COMPLETED';
export interface JobDefinition { key: string; displayName: string; description: string; category: string; queue: string; priority: number; timeoutMs: number; cancellationSupported: boolean; checkpointSupported: boolean; scheduleSupported: boolean; requiredPermissions: string[]; handlerKey?: string; }
export interface JobExecution { executionId: string; jobKey: string; queue: string; status: JobExecutionStatus; priority: number; attemptCount: number; progress: number; scheduledAt: string | null; startedAt: string | null; completedAt: string | null; correlationId: string | null; workerId: string | null; errorCode: string | null; errorMessage: string | null; checkpoint: Record<string, unknown>; deadLetter: boolean; createdAt: string; }
export interface JobSchedule { scheduleId: string; jobKey: string; name: string; type: string; status: string; nextRunAt: string | null; lastRunAt: string | null; timezone: string; overlapPolicy: string; }
